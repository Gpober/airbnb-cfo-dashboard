# Kalshi hourly BTC bot dependencies
requests>=2.31
cryptography>=41.0        # RSA-PSS request signing
websocket-client>=1.6     # optional: WebSocket feed (bot falls back to REST if absent)
anthropic>=0.116         # optional: AI decision layer (bot runs on rules if absent/no key)
